<?php error_reporting(0);include("includes/connect_db.php");$ipaddress = $_SERVER['REMOTE_ADDR'];  $fetch = $con->query("SELECT * FROM admin_ip WHERE ip = '$ip'");if($fetch->rowCount() > 0){	echo "<h1>Now you can access the Admin Panel</h1>";	echo "<b>Click to login:<b/>"	?><a href="login.php" class="btn btn-primary">login</a><?php}else{	$sql=$con->prepare("INSERT INTO admin_ip (ip) VALUES ('$ipaddress')");        	if($sql->execute()){		echo "<h1>Now you can access the Admin Panel</h1>";		echo "<b>Click to login:<b/>"		?><a href="login.php" class="btn btn-primary">login</a><?php		setTimeout(function(){ header("location: login.php"); }, 4000);	}} ?>